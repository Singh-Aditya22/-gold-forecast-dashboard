yfinance>=0.2.40
mftool>=2.0.5
duckdb>=0.10.0
pandas>=2.0.0
numpy>=1.26.0
prophet>=1.1.5
pmdarima>=2.0.4
xgboost>=2.0.0
lightgbm>=4.3.0
scikit-learn>=1.4.0
streamlit>=1.35.0
plotly>=5.22.0
# torch is intentionally not pinned here -- install separately with the CPU-only index
# (see README) to avoid pulling unnecessary CUDA wheels: pip install torch --index-url
# https://download.pytorch.org/whl/cpu
